var canvas-view = require('../canvas-view.js');

var ok = function(a, msg) { if (!a) throw new Error(msg || "not ok"); };

describe('canvas-view', function() {
  describe('#', function() {
    it('', function() {
      
    });
  });
});
